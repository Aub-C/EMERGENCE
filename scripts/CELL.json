{
  "$schema": "../protocol/cell.schema.json",
  "protocol_version": 1,
  "id": "platform.repository-intelligence",
  "name": "Repository Intelligence",
  "status": "active",
  "purpose": "Discover self-describing cells and compile bounded, machine-readable context packets for autonomous agents.",
  "scope": [
    "scripts/catalog.mjs",
    "scripts/orient.mjs",
    "scripts/preflight.mjs",
    "scripts/lib/cells.mjs",
    "scripts/CELL.json",
    "protocol/cell.schema.json",
    "test/orientation.test.mjs",
    "docs/SCALING_ARCHITECTURE.md",
    "docs/adr/0002-federated-cell-architecture.md"
  ],
  "entrypoints": [
    "scripts/catalog.mjs",
    "scripts/orient.mjs",
    "scripts/preflight.mjs"
  ],
  "interfaces": [
    {
      "name": "cell-catalog",
      "kind": "cli-json",
      "contract": "npm run catalog emits a JSON catalog of every valid CELL.json or exits non-zero."
    },
    {
      "name": "agent-orientation",
      "kind": "cli-json",
      "contract": "npm run orient -- --cell <id> or --path <path> emits a bounded orientation packet."
    },
    {
      "name": "mutation-preflight",
      "kind": "cli-text",
      "contract": "npm run preflight classifies the working diff against the canonical base and reports which paths the contributor may fix and which are the owner's. Exits 0 only when the mutation can merge unreviewed."
    }
  ],
  "depends_on": [
    "governance.admission-gate"
  ],
  "capabilities": [
    "discover-cell-manifests",
    "validate-cell-identities",
    "map-paths-to-cells",
    "compile-bounded-agent-context",
    "report-cell-dependencies-and-dependents",
    "classify-a-working-diff-before-submission"
  ],
  "read_first": [
    "docs/SCALING_ARCHITECTURE.md",
    "docs/adr/0002-federated-cell-architecture.md",
    "protocol/cell.schema.json",
    "scripts/lib/cells.mjs",
    "scripts/orient.mjs",
    "test/orientation.test.mjs",
    "scripts/preflight.mjs"
  ],
  "validation": [
    "npm run validate",
    "npm run catalog",
    "npm test"
  ],
  "security": {
    "risk": "medium",
    "network": "No network access.",
    "filesystem": "Read-only repository traversal and metadata reads.",
    "subprocesses": "Runs read-only git commands only: log, diff, ls-files, rev-parse. No writes to the repository and no network access.",
    "secrets": "None required or permitted."
  },
  "extension_points": [
    "Add contract graph and cross-cell import analysis.",
    "Add token-budget-aware context packing.",
    "Add remote cell coordinates when the project later federates across repositories."
  ],
  "decisions": [
    "docs/adr/0002-federated-cell-architecture.md"
  ],
  "replaced_by": null,
  "metadata": {
    "generation_created": 0
  }
}
